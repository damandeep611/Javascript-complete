"use strict"; // this will treat all JS code as newer version

// alert(3 + 3); //we are using nodejs, not browser

console.log(3 + 3);
// code readability should be high

let name = "daman";
let age = 20;
let isLoggedIn = true;
let state = null;

// data types
// number
// bigint => this is used for big number i.e stock market trading etc
// string  => " "
// boolean => true/false
//null => standalone value
// undefined => value is not assigned yet
// symbol => used in react for making components i.e unique components

// object =>

console.log(typeof state);
